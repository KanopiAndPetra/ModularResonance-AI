# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/

# RawData (separately tracked, see ModularResonance-AI-RawData/README.md)
ModularResonance-AI-RawData/

# Large JSON guard (results > 1MB tracked normally; flag anything larger)
# (uncomment to enforce)
# python_code/*.json diff=json
